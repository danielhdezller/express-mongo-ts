/**
 * Interface of the request records.
 * @export
 * @interface IRequestRecords
 */
export interface IRequestRecords {
  startDate: string;
  endDate: string;
  minCount: number;
  maxCount: number;
}
